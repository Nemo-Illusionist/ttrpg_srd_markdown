# Тестовый файл: проблемы заголовков

## Корректный раздел

Текст под корректным заголовком.

#### Пропущены уровни

Этот заголовок `####` идёт сразу после `##`, пропущен `###`.

#Нет пробела после решётки

Этот заголовок без пробела после `#`.

## Дубликат
## Дубликат

Два одинаковых заголовка подряд.

##Ещё без пробела

Тоже ошибка.

### Корректный подзаголовок (не должен давать ошибок)

Нормальный текст.

#### Корректный подподзаголовок (не должен давать ошибок)

Нормальный текст.
Без пустой строки перед заголовком.
## Заголовок без пустой строки выше

Это тоже ошибка — перед `##` нет пустой строки.
